# S3 (Simple Storage Service)

## 📝 정의
S3는 **AWS의 객체 스토리지 서비스**입니다. 파일을 저장하고 언제 어디서나 접근할 수 있습니다.

### 핵심 개념
- 버킷: 파일 저장 공간
- 객체: 저장되는 파일
- 내구성: 99.999999999% (11 nines)

## 💡 S3 사용

**AWS CLI**:
```bash
# 버킷 생성
aws s3 mb s3://my-bucket

# 파일 업로드
aws s3 cp myfile.txt s3://my-bucket/

# 파일 다운로드
aws s3 cp s3://my-bucket/myfile.txt ./

# 버킷 내용 보기
aws s3 ls s3://my-bucket/
```

**Python boto3**:
```python
import boto3

s3 = boto3.client('s3')

# 파일 업로드
s3.upload_file('local.txt', 'my-bucket', 'remote.txt')

# 파일 다운로드
s3.download_file('my-bucket', 'remote.txt', 'local.txt')

# 파일 목록
response = s3.list_objects_v2(Bucket='my-bucket')
for obj in response['Contents']:
    print(obj['Key'])
```

## 🎯 S3 활용

**정적 웹 호스팅**:
```
HTML, CSS, JS 파일 업로드
→ 웹사이트로 공개
→ CloudFront와 연동
```

**백업 스토리지**:
```
데이터베이스 백업
로그 파일 보관
→ 저렴한 비용
```

**이미지/비디오 저장**:
```
사용자 업로드 파일
→ CDN으로 배포
→ Lambda로 자동 처리
```

## 📊 스토리지 클래스

**S3 Standard**:
```
자주 접근하는 데이터
$0.023/GB/월
```

**S3 Infrequent Access**:
```
가끔 접근하는 데이터
$0.0125/GB/월
```

**S3 Glacier**:
```
아카이빙 용도
$0.004/GB/월
```

## 🚨 보안

```python
# 버킷 정책
{
  "Version": "2012-10-17",
  "Statement": [{
    "Effect": "Allow",
    "Principal": "*",
    "Action": "s3:GetObject",
    "Resource": "arn:aws:s3:::my-bucket/*"
  }]
}

# 암호화
s3.put_object(
    Bucket='my-bucket',
    Key='secret.txt',
    Body='secret data',
    ServerSideEncryption='AES256'
)
```

## 🔗 관련 용어
- [[AWS]]: S3의 제공자
- [[CloudFront]]: S3 + CDN
- [[Lambda]]: S3 이벤트 처리
- [[EBS]]: 블록 스토리지

---
*카테고리: 클라우드*
