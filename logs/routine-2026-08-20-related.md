# 2026-08-20 · vocab-related (관련 용어 따라가기)

**후보 없음. 추가 0편.**

전략 문단이 미리 경고한 그대로다 — 기존 단어의 `🔗 관련 용어` 에 나오는 이름이
전부 이미 자기 파일을 갖고 있었다. 억지로 채우지 않고 끝낸다.

## 어떻게 세었나

`content/` 아래 `.md` 329편 전부를 읽고, 각 편의 `## 🔗 관련 용어` 섹션에서
`[[...]]` 이름을 뽑아 기존 단어와 대조했다. 대조 축은 본문 3번 단계가 시키는 세 가지다.

- 파일명
- H1 제목에서 괄호를 뺀 본체 (`# DNS (Domain Name System)` → `DNS`)
- H1 괄호 안 원어 (`Domain Name System`)

정규화는 NFKC → 소문자 → 공백/하이픈/밑줄/`·`/구두점 제거.

결과:

```
관련 용어 섹션이 있는 파일   329 / 329
고유 related 이름            274
그중 기존 단어로 해소된 것    274
자기 파일이 없는 이름           0
```

## 범위를 한 번 넓혀서 재확인

관련 용어 섹션만 보면 놓칠 수 있으니, 같은 대조를 **본문 전체의 `[[링크]]** 로
넓혀서 한 번 더 돌렸다. 미매칭은 딱 하나 나왔다.

| 미매칭 | 나온 곳 | 판정 |
|---|---|---|
| `[[용어]]` | `content/IT_Expert_로드맵.md` | 실제 단어가 아니라 백로그 문서의 자리표시자. 후보 아님 |

즉 앱에서 눌러도 아무 데도 가지 않는 죽은 링크는 이 하나뿐이고, 그것도 단어
노트가 아니라 로드맵 문서 안에 있다. related 전략이 채울 빈칸은 0개가 맞다.

## 권별 단어 수 (`build.py --dry-run`, 시작 = 종료 시점)

전체 329개, 변동 없음.

```
컴퓨터과학 기초 40   프로그래밍 20   네트워크 43   웹 개발 34
데이터베이스 34      아키텍처 패턴 20   보안·인증 35   클라우드 13
인프라·운영 13       개발 도구 31      AI·LLM 27     제품 관리 19
```

상한(60)에 닿은 권은 없다. 가장 얇은 권은 여전히 클라우드·인프라(각 13)다.

## 검토했지만 만들지 않은 것

만들 후보 자체가 없었으므로 버린 단어도 없다. 인용한 출처도 없다.
`build.py` / `verify_new_terms.py` 는 추가가 없으므로 부르지 않았다
(`--dry-run` 만 현황 파악용으로 한 번 돌렸고, `data/` 는 건드리지 않았다).

## 이 문서나 도구가 틀렸다고 느낀 점

**related 전략은 구조적으로 빈손이 기본값이다.** 이번이 우연히 0개인 것이
아니라, 지금 저장소의 규약이 0개를 강제한다. `check_template.py` 가 관련 용어의
`[[링크]]` 를 실재하는 단어로 요구하는 이상, 새 단어를 쓰는 사람은 이미 있는
단어만 링크하게 된다. 그러면 미해소 후보는 원리상 생기지 않는다.
지난 두 실행(roadmap·trend)이 넣은 4편도 마찬가지로 기존 단어만 링크했다.

사람이 볼 만한 선택지는 두 가지로 보인다. 어느 쪽이든 `tools/` 를 고치는 일이라
여기서는 기록만 남긴다.

1. **금요일 전략을 바꾼다.** related 대신 "얇은 권 채우기"(클라우드·인프라 13개)
   같은 다른 각도를 주는 편이 매주 한 번의 실행을 살린다.
2. **후보 공급원을 넓힌다.** 관련 용어 섹션 대신, 본문에서 반 구절로 풀어 쓴
   낯선 용어(`벡터(뜻을 담은 숫자 목록)` 꼴)를 후보로 삼게 하면 풀이 다시 생긴다.
   다만 이건 링크가 아니라서 기계로 뽑기 어렵고, 판단이 많이 섞인다.

지시 자체가 틀린 곳은 없었다. 전략 문단이 "후보가 0개면 아무것도 만들지 마라" 를
미리 적어 둔 덕분에 이번 실행은 헤매지 않았다.
