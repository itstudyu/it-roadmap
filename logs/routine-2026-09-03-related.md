# 루틴 실행 기록 — 2026-09-03 (related)

전략: **관련 용어 따라가기 (vocab-related)**
결과: **후보 없음 — 추가한 단어 0개**

## 권별 단어 수 (`python3 tools/build.py --dry-run`)

단어장 12권, 단어 638개

| 권 | 단어 수 |
|---|---|
| 컴퓨터과학 기초 | 43 |
| 프로그래밍 | 41 |
| 네트워크 | 76 |
| 웹 개발 | 34 |
| 데이터베이스 | 69 |
| 아키텍처 패턴 | 48 |
| 보안 · 인증 | 84 |
| 클라우드 | 61 |
| 인프라 · 운영 | 80 |
| 개발 도구 | 32 |
| AI · LLM | 42 |
| 제품 관리 | 28 |

## 후보 조사

모든 노트의 `## 🔗 관련 용어` 섹션에서 `[[링크]]` 를 모아, build.py 의 실제
별칭 해석(`collect` → `assign_ids` → `widen_aliases` 로 만들어지는 alias 집합)에
대조했다. 앱이 회색 글자로 렌더링하는(= 자기 노트가 없는) 링크만 후보가 된다.

- 관련 용어 섹션의 서로 다른 링크: **539개**
- 그중 해석되지 않는(자기 파일이 없는) 링크: **0개**

즉 모든 관련 용어가 이미 기존 단어로 해소된다.

참고로 `widen_aliases`(복합 이름 조각 별칭) 를 빼고 세면 딱 하나
`Materialized View` 만 미해석으로 잡히는데, 이는 `content/데이터베이스/View &
Materialized View.md` 노트가 `&` 기준으로 쪼개져 "Materialized View" 별칭을
이미 갖고 있기 때문이다. 자기 자리가 있으므로 진짜 후보가 아니다.

## 결론

후보가 0개이므로 새 단어를 만들지 않았다. 전략 지침(프롬프트 및 문서 맨 아래
`vocab-related` 문단)대로 억지로 지어내지 않고 이 기록만 남기고 끝낸다.
빈손으로 끝나는 것은 실패가 아니다 — 근거 없는 단어를 넣는 것이 실패다.

## 도구/문서에 대한 의견

- 이번 실행에서 문서·검사기가 어긋난 점은 발견하지 못했다. related 전략의
  "후보 없음" 경로가 프롬프트에 명시돼 있어 판단이 명확했다.
