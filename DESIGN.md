# 디자인 시스템

전부 `css/tokens.css` 에 CSS 변수로 들어 있다. 컴포넌트에는 raw hex 를 쓰지 않는다.
색을 바꾸고 싶으면 그 파일만 고치면 된다.

## 방향

> 종이처럼 차분한 바탕에 잉크로 쓴 사전. 읽는 것이 주인공이고 UI 는 물러난다.

원하는 인상: clean, calm, premium, editorial, focused, low cognitive load
피한 것: AI SaaS 랜딩 스타일, 과한 그라디언트, 의미 없는 유리 효과, 큰 그림자, 게임 UI

## 색

**브랜드 색을 따로 두지 않았다.** 가장 진한 잉크색이 곧 주요 액션 색이다.
학습 앱에서는 정답 초록과 오답 빨강이 이미 자리를 차지한다.
거기에 브랜드 색까지 넣으면 화면에 색이 셋이 되고, 무슨 색이 무슨 뜻인지 흐려진다.
그래서 색은 **학습 상태를 말할 때만** 쓴다.

### 표면과 글자

| 토큰 | 밝은 화면 | 어두운 화면 | 쓰임 |
|---|---|---|---|
| `--paper` | `#fbfaf8` | `#141310` | 앱 배경. 순백이 아니라 살짝 따뜻하다 |
| `--surface` | `#ffffff` | `#1c1b17` | 카드 |
| `--surface-sunken` | `#f3f1ec` | `#232219` | 입력창, 코드 블록 |
| `--line` | `#e9e5de` | `#2f2d26` | 기본 헤어라인 |
| `--line-strong` | `#d5cfc4` | `#45423a` | 강조 구분선 |
| `--ink` | `#1a1917` | `#f2efe8` | 본문, 표제어, 주요 버튼 배경 |
| `--ink-2` | `#57524a` | `#b0aa9c` | 보조 텍스트 |
| `--ink-3` | `#726c60` | `#8b8578` | 메타, 캡션 |

대비: `--ink` 15.6:1, `--ink-2` 7.4:1, `--ink-3` 4.9:1. 모두 WCAG AA 를 넘는다.
`--ink-3` 는 4.5:1 을 겨우 넘기므로 캡션 이하로만 쓴다.

### 학습 상태

색만으로 뜻을 전달하지 않는다. 항상 아이콘과 글자를 같이 쓴다.
목록에서는 자리를 아끼려고 점만 쓰되, `aria-label` 로 상태 이름을 준다.

| 상태 | 뜻 | 색 (밝은 화면) | 점의 모양 |
|---|---|---|---|
| `new` | 아직 공부하지 않음 | `#726c60` | 빈 원 |
| `reading` | 읽는 중 | `#2c5f7c` | 채운 원 |
| `learned` | 학습 완료 | `#3a6b4c` | 도넛 |
| `passed` | 퀴즈 통과 | `#235c43` | 채운 원 |
| `review` | 복습 필요 | `#8a5a18` | 채운 원 |

`learned` 를 도넛으로 만든 이유가 있다. 처음에는 테두리만 있는 초록 원으로 뒀는데,
8px 크기에서 테두리만 있는 회색 원(`new`)과 구분이 되지 않았다.
색이 아니라 형태를 다르게 만들어야 작은 크기에서 읽힌다.

### 피드백

| 토큰 | 밝은 화면 | 어두운 화면 |
|---|---|---|
| `--ok` | `#235c43` | `#9ad3b2` |
| `--danger` | `#a33a31` | `#e08b81` |

## 타이포

```
--font-sans  Pretendard -> Apple SD Gothic Neo -> 시스템
--font-head  Source Serif 4 -> Iowan Old Style -> Georgia   (표제어 전용)
--font-mono  SF Mono -> JetBrains Mono                       (원어 표기, 숫자)
```

웹폰트는 CDN 에서 가져오되 시스템 폰트로 떨어지게 해뒀다. 오프라인에서도 화면은 그대로 동작한다.

**표제어에만 세리프를 쓴다.** 사전이라는 장르의 관습이고 (OED, Apple 사전),
영문 용어와 한글 설명을 시각적으로 분리해준다. 표제어는 대부분 영문이라
한글 세리프 폰트 대체 문제도 생기지 않는다. 나머지는 전부 산세리프다.

### 크기

11 · 12 · 13 · 14 · 15 · 16 · 17 · 20 · 24 · 28 · 34

본문 최소는 16px 이다. 그보다 작으면 iOS 가 입력창에서 화면을 자동 확대한다.
긴 설명은 17px 로 읽는다.

### 한글 조판

```
word-break: keep-all;     어절 단위로 줄을 바꾼다
text-wrap: balance;       제목의 줄 길이를 고르게
text-wrap: pretty;        본문 마지막 줄에 한 단어만 남는 것을 피한다
```

`word-break` 기본값은 글자 단위로 끊어서 "네트워" / "크" 처럼 갈라진다.
한글은 어절 단위로 끊어야 읽힌다.

**작은 라벨에 `text-transform: uppercase` 를 쓰지 않는다.**
한글에는 대소문자가 없어서 아무 효과가 없다. 영문 UI 패턴을 그대로 옮겨온 흔적일 뿐이다.
같은 이유로 11~12px 한글에 자간을 벌리지 않는다. 오히려 읽기 나빠진다.
`--tracking-label` 은 라틴 문자만 있는 자리에서만 쓴다.

### 행간

```
--lh-body   1.72   본문. 한글은 라틴 문자보다 행간이 더 필요하다
--lh-snug   1.45   카드 안의 짧은 글
--lh-tight  1.25   제목
--lh-head   1.1    표제어
```

본문 최대 폭은 `34rem` 이다. 한 줄에 40~50자가 들어간다.

## 간격

4px 배수. `4 8 12 16 20 24 32 40 48 64`

화면 좌우 여백(`--gutter`)은 20px 이다.
세로 리듬은 항목 사이 12, 블록 사이 32, 큰 구획 사이 40 을 쓴다.

## 모서리

한 가지 규칙만 쓰고 그 밖의 값은 쓰지 않는다.

```
--r-chip       8px    칩, 작은 조각
--r-control   12px    버튼, 입력창, 퀴즈 선택지
--r-container 16px    카드, 시트
--r-full      999px   상태 배지
```

## 그림자

거의 쓰지 않는다. 계층은 헤어라인과 여백으로 만든다.
쓸 때는 배경의 따뜻한 색조를 섞는다. 순수 검정 그림자는 밝은 배경에서 탁해 보인다.

```
--shadow-raised  0 1px 2px rgba(26,25,23,.05)
--shadow-sheet   0 -2px 16px -6px rgba(26,25,23,.1)   피드백 시트 전용
```

## 아이콘

Lucide (ISC) 의 24x24 스트로크 지오메트리를 인라인 SVG 로 넣었다.
빌드 도구 없이 `file://` 로 열려야 해서 패키지 대신 인라인을 골랐다.

- 굵기 1.75 로 통일
- 크기는 13 / 14 / 15 / 18 / 20 / 22 만 쓴다
- **이모지를 아이콘으로 쓰지 않는다.** 플랫폼마다 모양이 달라지고 토큰으로 제어할 수 없다.
  (원본 노트의 이모지는 콘텐츠이므로 그대로 둔다. 제목의 이모지만 걷어낸다.)

## 컴포넌트

### 버튼

높이 48px, 모서리 12px. 누르면 `scale(0.985)` 로 눌리는 느낌을 준다.

- **primary** 배경 `--ink`, 글자 `--ink-inverse`. 한 화면에 하나만 둔다.
- **secondary** 흰 배경에 `--line-strong` 테두리
- **ghost** 배경 없음. 목록 안의 보조 동작

아이콘만 있는 버튼에는 반드시 `aria-label` 을 단다.
공간이 있으면 글자를 같이 둔다.

### 카드

카드는 계층이 실제로 있을 때만 쓴다. 그 외에는 헤어라인(`.rows`)으로 묶는다.
카드를 남발하면 화면이 상자 무더기가 된다.

### 진행 표시

두 가지를 쓴다.

- **단색 막대** 퀴즈 진행률처럼 한 가지 값일 때
- **누적 막대** 단어장 진행률. 퀴즈 통과 / 학습 완료 / 읽는 중을 한 막대 안에서 구분한다.
  "얼마나 했는가"가 아니라 "어디까지 갔는가"를 보여주기 위해서다.

### 퀴즈 선택지

최소 높이 60px, 모서리 12px, 항목 사이 12px.
엄지로 누르기 쉬워야 하고, 옆 항목을 잘못 누르면 안 된다.

답을 고르면 정답은 항상 초록으로 표시된다. 틀렸을 때 뭐가 맞는지 모르면 배우지 못한다.
고른 오답은 빨강, 나머지는 투명도 0.45 로 물러난다.

### 내비게이션

- 하단 탭 4개. 아이콘과 글자를 항상 같이 보여준다.
- 현재 위치는 색과 굵기로 표시한다.
- **읽기 화면과 퀴즈 진행 화면에서는 탭 바를 감춘다.** 하나의 일에 집중하는 자리다.
  대신 화면에 맞는 하단 액션 바가 들어간다.
- 뒤로 가면 보던 스크롤 위치로 돌아간다.

## 브라우저가 그리는 부분

선택 영역, 텍스트 커서, 스크롤바, 탭 하이라이트는 손대지 않으면 브라우저 기본값으로 남는다.
그 기본값은 어느 디자인 시스템에도 속하지 않는다. 나머지가 아무리 정돈돼 있어도
여기서 "조립한 페이지" 티가 난다. 전부 팔레트 안으로 끌어왔다.

```
::selection              --st-review-bg (본문 강조와 같은 색)
caret-color              --ink
accent-color             --ink
scrollbar                --line-strong, 얇게, 트랙 투명
-webkit-tap-highlight    없앰 (기본 파란 사각형)
text-underline-offset    0.18em
```

## 모션

```
--dur-fast  120ms   눌림 반응
--dur       180ms   상태 변화
--dur-slow  260ms   화면 전환, 시트
--ease      cubic-bezier(0.32, 0.72, 0, 1)
```

움직임은 인과를 전달할 때만 쓴다. 장식으로 쓰지 않는다.

**화면마다 똑같은 등장 효과를 뿌리지 않는다.** 그러면 어디를 봐야 할지 알 수 없고
매번 기다리게 된다. 대신 이 제품의 장면 하나에 집중한다.

### 한 장면: 답을 고른 순간

퀴즈에서 답을 누르면 세 가지가 동시에 일어난다.

- 정답이 자리를 잡는다 (`settle`, 420ms). 크기가 1.018 까지 갔다가 돌아온다
- 틀린 답은 짧게 한 번 흔들린다 (`nudge`, 300ms). 알리되 나무라지 않는 정도로
- 나머지 보기는 순서대로 물러난다 (`recede`, 45ms 씩 지연). 시선이 정답으로 모인다
- 해설 시트가 올라오고, 뒤를 옅은 막이 덮는다

전부 이미 보이는 상태에서 시작한다. 콘텐츠가 늦게 나타나는 일은 없다.

### 또 한 장면: 뜻을 확인한 순간

떠올리기 카드에서 답은 아래에서 올라오지 않는다. 제자리에서 흐림이 걷히며
드러난다 (`reveal`, 260ms, `filter: blur(3px) → 0`).

뜻을 **받는** 동작이 아니라 원래 거기 있던 것을 **확인하는** 동작이기 때문이다.
아래에서 밀어 올리면 새 정보가 도착한 것처럼 읽히고, 그건 이 화면이 하려는 말과 다르다.

### 그 밖의 움직임은 기능만 한다

- 앞으로 갈 때는 오른쪽에서, 뒤로 올 때는 왼쪽에서 들어온다. 방향이 위치를 말해준다.
- 접이식 섹션은 열 때만 움직인다. 요청한 내용이 도착하는 것을 보여준다.
- `transform` 과 `opacity`, `filter` 만 애니메이션한다.
  `width` / `height` / `padding` 은 매 프레임 레이아웃을 다시 계산시킨다.
- `prefers-reduced-motion` 을 켠 사용자에게는 전부 즉시 상태로 준다.

## 노트 안의 코드펜스를 세 갈래로 나눈다

Obsidian 노트의 ``` 블록 167개 중 **113개는 코드가 아니다.** 언어 태그 없이
이모지와 화살표로 개념의 흐름을 그린 도해이거나, 공백으로 자리를 맞춘 표다.
전부 `<pre><code>` 로 내보내면 차분하게 짜놓은 화면을 콘텐츠가 뚫고 나온다.

원본 Markdown 은 건드리지 않는다. 읽는 쪽에서 나눈다.

| 판정 | 근거 | 조판 |
|---|---|---|
| 진짜 코드 | 언어 태그가 있거나 줄 첫머리가 코드 키워드 | 고정폭 유지 + 언어 칩 |
| 개념 도해 | 화살표나 톤 이모지가 있다 | 본문 폰트, 화살표는 거터로 |
| 정렬 표 | 두 칸 이상 띄운 2열이 70% 이상 | `dt` 고정폭 / `dd` 본문 폰트 |

### 이모지는 지우지 않고 톤으로 올려 보낸다

저자가 `😱` 로 표시한 건 "문제 상황"이라는 뜻이다. 그 뜻을 색과 테두리로 옮긴다.

```
😱 ❌ ✗   →  --bad    danger 계열 테두리 + 옅은 배경
⚠         →  --warn   review 계열
✅ ✓      →  --good   ok 계열
```

**블록 전체의 톤은 줄들의 톤이 한 방향일 때만 인정한다.** 한 블록 안에서
✅ 와 ❌ 를 대조하는 노트가 많은데(105개 중 56개), "첫 이모지가 이긴다"로 하면
한계를 설명하는 블록이 초록색이 된다. 의견이 갈리면 블록은 중립으로 두고
판정을 줄에 남긴다. 블록 톤과 줄 톤을 같이 쓰지는 않는다 — 같은 말을 두 번 하게 된다.

## 접힌 섹션에는 안에 든 것의 이름을 적는다

처음에는 "첫 문장"을 미리보기로 쓰려 했는데, 노트를 열어 보니 이 섹션들에는
산문이 거의 없다. 소제목·비교표·목록이 본체다.

```
HTTP 메서드          GET (조회) · POST (생성) · PUT (전체 수정) · PATCH…
HTTP/1.1 vs 2 vs 3   특성 · HTTP/1.1 · HTTP/2 · HTTP/3
관련 개념 3개         REST API · TCP · DNS
```

어설픈 한 문장보다 열지 말지 고르는 데 훨씬 도움이 된다.
첫 섹션은 열어둔다 — 전부 접혀 있으면 들어왔을 때 읽을 게 요약 한 장뿐이다.

## 모바일 기준

처음부터 세로 화면(390 x 844)을 기준으로 만들었다.

- 터치 영역 최소 44px. 아이콘 버튼은 눈에 보이는 크기보다 탭 영역을 넓게 잡는다.
- 하단 고정 요소는 `env(safe-area-inset-bottom)` 을 넣어 제스처 바를 피한다.
- `min-height: 100dvh` 를 쓴다. `100vh` 는 iOS 주소창 때문에 화면이 튄다.
- 가로 스크롤은 만들지 않는다. 표와 코드 블록만 자기 안에서 가로로 넘긴다.
- 확대를 막지 않는다.
- 768px 이상에서는 캔버스가 40rem 으로 넓어지고 양옆에 경계선이 생긴다.
  데스크톱용 레이아웃을 따로 만들지는 않았다. 이 앱은 손에 들고 쓰는 물건이다.

## 접근성

- 색만으로 뜻을 전달하지 않는다. 상태는 색 + 아이콘 + 글자.
- 진행 막대에 `role="progressbar"` 와 값, 누적 막대에는 설명이 담긴 `aria-label`.
- 화면이 바뀌면 제목으로 초점을 옮긴다. 다만 눈에 보이는 테두리는 그리지 않는다.
  키보드로 이동할 때의 초점 표시(`:focus-visible`)와는 다른 처리다.
- 검색 입력은 16px 이상. 그보다 작으면 iOS 가 화면을 확대한다.
- 밝은 화면과 어두운 화면을 각각 확인했다. 한쪽 값으로 다른 쪽을 추측하지 않았다.
